1.  Apply functional programming concepts to run statistical analyses
    that fit within the `{targets}` pipeline framework, regardless of
    what statistical method is used.
2.  Use the `{broom}` package to extract the model results in a tidy
    form.
3.  Using what we learned ...a statistical analysis workflow into a
    reproducible pipeline using `{targets}`
